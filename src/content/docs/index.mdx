---
title: Quiote
description: A powerful, scalable PHP 8.5+ MVC framework.
template: splash
hero:
  tagline: A powerful, scalable PHP 8.5+ MVC framework for serious, long-lived applications.
  actions:
    - text: Get Started
      link: /getting-started/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/quioteframework/quiote
      icon: external
      variant: minimal
---

import { CardGrid, Card, LinkCard } from '@astrojs/starlight/components';

<CardGrid stagger>
  <Card title="PSR-15 middleware pipeline" icon="seti:pipeline">
    A clean, composable request pipeline — routing, security, validation, CSRF, and dispatch each live in their own middleware.
  </Card>
  <Card title="Fine-grained control" icon="setting">
    Quiote puts choice and freedom over limiting conventions. Most implementation decisions stay yours.
  </Card>
  <Card title="Modern PHP 8.5+" icon="seti:php">
    Typed properties, enums, readonly, attributes, and first-class callable syntax throughout.
  </Card>
  <Card title="Flexible configuration" icon="document">
    PHP arrays, YAML, or XML — mixed and matched per file, with full inheritance across formats.
  </Card>
</CardGrid>

## A quick look

A request flows through, in order: **URL, route, action, view, template, response**. A route maps a path to a module and action:

```php title="Routing/AppRouting.php"
$routes->add('index', new Route('/', ['_module' => 'Default', '_action' => 'Index']));
```

The action decides *what happens* and returns the **name of a view** — never HTML. One class handles every verb: Quiote maps `GET` to `executeRead`, `POST` to `executeWrite`, `PUT`/`PATCH` to `executeUpdate`, `DELETE` to `executeRemove`.

```php title="Modules/Default/Actions/IndexAction.php"
class IndexAction extends Action
{
    public function executeRead(WebRequest $rd)
    {
        $this->setAttribute('title', 'Home');
        return 'Success';  // render the "Success" view for this action
    }
}
```

That naming convention is the thread that ties everything together — the `Index` action returning `'Success'` resolves to the `IndexSuccessView` class and the `IndexSuccess` template. [Build the whole thing, end to end](/getting-started/your-first-app/).

## Get started

The latest stable version is **v1.2.2**. Install it from Packagist with Composer:

```bash
composer require quioteframework/quiote
```

Then scaffold a fresh application:

```bash
vendor/bin/quiote new my-app
```

See [Installation](/getting-started/installation/) for requirements and the [command-line tool](/getting-started/cli/) for everything `quiote` can do.

## Explore

<CardGrid>
  <LinkCard title="Your first application" href="/getting-started/your-first-app/" description="A complete app end to end — front controller, route, action, view, template." />
  <LinkCard title="Design philosophy" href="/getting-started/philosophy/" description="Why Quiote favours an unopinionated core with opinionated drop-ins." />
  <LinkCard title="The request lifecycle" href="/architecture/request-lifecycle/" description="The full path a request takes, from the kernel to the emitted response." />
  <LinkCard title="The middleware pipeline" href="/architecture/middleware-pipeline/" description="How routing, security, validation, and dispatch compose as PSR-15 middleware." />
</CardGrid>

<Card title="Built for AI agents" icon="rocket">
  Quiote ships a first-class **MCP server** — written in Quiote itself — that gives Claude Code, Cursor, or any MCP client authoritative, up-to-date knowledge of the framework: the docs, full-text search, and convention cards. [Set up the AI assistant](/getting-started/mcp-assistant/).
</Card>
